
    <%= user.name + "       "%>

<% if Follow.find_by(followee_id: user.id, follower_id: current_user.id).nil? %>
    <turbo-frame id=<%="send_follow_#{user.id}"%>>
        <%= link_to "Send Follow Request", follows_path(user_id: user.id), data: { 'turbo-method' => :post } %>
    </turbo-frame>
<% end %>

<br>
