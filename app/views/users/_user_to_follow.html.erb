<%= user.name + "       "%>


<% if Follow.find(followee_id: user.id, follower_id: current_user.id) == nil %>
    <%= "Pending follow" %>
<% elsif user.approved != true %>
    <%= link_to "Follow", follow_path(user.id), data: { 'turbo-method' => :post } %>
<% end %>