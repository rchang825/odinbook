<%= link_to "Log In", new_user_session_path %>
|
<%= link_to "Log Out", destroy_user_session_path, data: { turbo_method: :delete } if user_signed_in? %>
<br>
<%= "Hello, " + current_user.name if user_signed_in? %>