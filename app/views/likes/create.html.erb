<turbo-frame id=<%="like_from_#{current_user.id}_on_#{@like.likeable_type}_#{@likeable.id}"%>>
    <%= button_to "Refresh post", "/", method: :get, data: { turbo_frame: "#{@like.likeable_type}_#{@likeable.id}"}%>
</turbo-frame>
